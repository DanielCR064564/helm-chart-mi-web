apiVersion: v1
kind: Service
metadata:
  name: {{ include "mi-web.fullname" . }}
  labels:
    {{- include "mi-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mi-web.selectorLabels" . | nindent 4 }}
